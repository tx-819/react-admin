# ECS / 服务器上使用：与 workflow 写入的 .env 配合（DOCKER_IMAGE）
# 首次部署前在服务器创建目录，workflow 会 scp 本文件并 ssh 执行 pull/up。
services:
  web:
    image: ${DOCKER_IMAGE}
    ports:
      - "${HTTP_PORT:-80}:80"
    restart: unless-stopped
